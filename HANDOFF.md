# HANDOFF — current working state (living doc)

Last updated: 2026-06-11. Read this first when picking the project back up in a
fresh session. Conventions live in `/Users/dantoombs/Downloads/CLAUDE.md`; this
is the *what-are-we-doing-right-now* layer on top.

## TL;DR
Branch **`seo-aeo-skill`** (PR #2 open to `main`, NOT merged). Executing the SEO
deployment plan plus a UX / "de-AI" polish pass for the Mediations Australia site.
Everything committed and pushed. Site is pre-launch, held **noindex** on Vercel.

## Where things live
- Repo: `github.com/FastFirms/mediations`. Working dir: `.../Google Ads/mediations`.
- Deploy: Vercel (`dan-practiceproos-projects`). `main` → staging; branch push →
  preview URL. Previews sit behind Vercel auth (anonymous curl gets 401 — view in a
  logged-in browser). Always hand over the newest commit's preview URL.
- Static site generated by Python in `build/`. **Edit the generators, never the
  built `*/index.html`.** Rebuild chain + validation one-liner are in CLAUDE.md §5.

## Done in this work stream
- New commercial hub **`/divorce-mediation/`** (gen_services.py); added to `SERVICES`
  so it auto-wires into nav, footer and sitemap.
- Retuned `/family-law-mediation/` (now owns "family mediators / family mediation
  services"); strengthened `/sydney-mediation/` + the shared location template
  (per-city "family/divorce/property/parenting mediation in {city}" section).
- 8 supporting articles in **`gen_blog_batch11.py`**, registered in `check_authority`
  BLOG list, `gen_blog_index.py`, and `gen_meta.py`: who-pays-for-mediation,
  private-vs-free-mediation, divorce-mediator-vs-divorce-lawyer, mediation-before-divorce,
  what-happens-if-mediation-fails, what-is-in-the-property-pool, delaying-property-settlement,
  co-parenting-mediation.
- Deepened the 3 thin hubs (family-law ~997, property ~1,037, parenting ~886 words).
- **Redirects ported into `vercel.json`** (213, audited; fixed 3 backwards/dead targets,
  collapsed 6 duplicate sources). The `.htaccess` is dormant on Vercel; same 3 bugs
  fixed there too. **The noindex header was preserved** in the same file.
- UI/UX: removed decorative green callout boxes; white button text; homepage "court
  path" readability; global table styling (`figure.tbl` in styles.css) + roomier
  padding; **Services megamenu** redesigned into grouped columns (`SERVICE_GROUPS`).
- Image system: `img()` helper (templates.py) + `figure.photo` CSS +
  `assets/images/IMAGE-BRIEF.md` (licensed-stock brief with sizes/weights). No images
  wired yet.
- Design call: user **kept the default fonts/palette** (no re-skin). The agreed de-AI
  lever is **licensed stock imagery** — mood/context only, never posing as the real team.
- Last green validation: `104 pages | H1:0 | schema:0 | uniqueH1:104/104 | broken:NONE`
  and `check_authority: ALL POSTS MEET STANDARD`.

## In progress / next
- **HERO IMAGE (active task):** user has a licensed Australian Queenslander-verandah
  stock photo for the homepage hero. Steps: (1) save the master into `assets/images/`
  — it was NOT on disk at last check; (2) optimise — **this Mac has no cwebp/magick/Pillow,
  only `sips`**, so use `sips` → high-quality JPEG, no upscaling (avoid pixelation);
  (3) generate responsive sizes 2400/1600/1200/800; (4) wire into the homepage hero in
  `gen_home.py` and add `srcset` to the `img()` helper; (5) decide the hero text+image
  layout against the real image.
- Then: location heroes + About image per the brief.
- Launch prerequisites (when the user says go): remove the vercel.json noindex lock;
  trailing-slash redirect test on production; optional cleanup of ~70 pre-existing
  over-length meta titles/descriptions.
- PR #2 awaiting a merge decision.

## Hard guardrails — do not break
- **Never push to `main` without explicit ask. Never force-push.**
- **Never run `gen_migration.py`** (stale/destructive) or **`gen_booking.py`** (out of
  sync with the hand-wired Formspree action).
- **Never remove the `vercel.json` `X-Robots-Tag: noindex` lock.** That file now ALSO
  holds the redirects array — preserve both.
- Edit generators in `build/`, not the built HTML.
- R6 safety: surface **000** and **1800RESPECT** on family-violence pages, with the
  "mediation may not be appropriate" disclaimer.
- After any rebuild: run the validation one-liner + `check_authority.py` (must end
  `ALL POSTS MEET STANDARD`). Titles ≤60 chars, meta descriptions ≤160.

## Key files
- SEO deployment plan: `/Users/dantoombs/Downloads/mediations-australia-seo-deployment-plan.md`
- Conventions: `/Users/dantoombs/Downloads/CLAUDE.md`
- Full prior conversation (resume to restore everything):
  `~/.claude/projects/-Users-dantoombs-Downloads-Claude-Code-Google-Ads/69387014-f68e-40c5-9a76-efc99c4e316d.jsonl`
